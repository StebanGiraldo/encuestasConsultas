<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>EncuestasCiudadanas</title>
  <style>
    :root {
      --primario: #0056b3;
      --fondo-navbar: #f3f8ff;
      --borde-navbar: #e0e0e0;
      --hover-link: #d0e7ff;
      --boton-bg: #007bff;
      --boton-hover: #0091ff;
      --dropdown-hover: #f7f7f7;
    }

    body {
      margin: 0;
      font-family: Arial, sans-serif;
      background-color: #f9f9f9;
      color: #333;
    }

    nav.navbar {
      background: linear-gradient(90deg, #fff, var(--fondo-navbar));
      border-bottom: 1px solid var(--borde-navbar);
      padding: 15px 20px;
      display: flex;
      justify-content: center;
      align-items: center;
      position: relative;
      z-index: 100;
    }

    .navbar-container {
      width: 100%;
      max-width: 1200px;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .logo {
      font-size: 22px;
      font-weight: bold;
      color: var(--primario);
      text-decoration: none;
    }

    #menu-toggle {
      display: none;
    }

    .hamburger {
      display: none;
      width: 30px;
      height: 22px;
      position: relative;
      cursor: pointer;
      transition: all 0.3s ease-in-out;
    }

    .hamburger span {
      position: absolute;
      height: 3px;
      width: 100%;
      background-color: #333;
      border-radius: 3px;
      transition: all 0.3s ease;
    }

    .hamburger span:nth-child(1) { top: 0; }
    .hamburger span:nth-child(2) { top: 9px; }
    .hamburger span:nth-child(3) { top: 18px; }

    #menu-toggle:checked + .hamburger span:nth-child(1) {
      transform: rotate(45deg);
      top: 9px;
    }

    #menu-toggle:checked + .hamburger span:nth-child(2) {
      opacity: 0;
    }

    #menu-toggle:checked + .hamburger span:nth-child(3) {
      transform: rotate(-45deg);
      top: 9px;
    }

    .nav-links {
      list-style: none;
      display: flex;
      gap: 20px;
      margin: 0;
      padding: 0;
    }

    .nav-links li a {
      text-decoration: none;
      color: #333;
      font-size: 16px;
      padding: 8px 12px;
      transition: background 0.3s ease, border-radius 0.3s ease;
    }

    .nav-links li a:hover {
      background-color: var(--hover-link);
      border-radius: 5px;
    }

    .btn {
      background-color: var(--boton-bg);
      color: #fff;
      padding: 10px 16px;
      border-radius: 5px;
      text-decoration: none;
      font-weight: bold;
      transition: background 0.3s ease;
      margin-left: 5px;
    }

    .btn:hover {
      background-color: var(--boton-hover);
    }

    .dropdown {
      position: relative;
    }

    .dropbtn {
      background: transparent;
      color: #333;
      font-size: 16px;
      border: none;
      cursor: pointer;
      padding: 8px 12px;
    }

    .dropdown-content {
      display: none;
      position: absolute;
      background-color: #fff;
      min-width: 160px;
      right: 0;
      z-index: 1;
      box-shadow: 0 4px 6px rgba(0,0,0,0.1);
      border-radius: 6px;
    }

    .dropdown-content a {
      color: #333;
      padding: 10px 16px;
      text-decoration: none;
      display: block;
    }

    .dropdown-content a:hover {
      background-color: var(--dropdown-hover);
    }

    .dropdown:hover .dropdown-content {
      display: block;
    }

    /* Responsive */
    @media (max-width: 768px) {
      .hamburger {
        display: block;
      }

      .nav-links {
        flex-direction: column;
        position: absolute;
        top: 70px;
        right: -300px;
        width: 250px;
        background-color: white;
        border: 1px solid var(--borde-navbar);
        box-shadow: 0 4px 8px rgba(0,0,0,0.1);
        padding-bottom: 10px;
        transition: right 0.3s ease;
        opacity: 0;
      }

      #menu-toggle:checked ~ .nav-links {
        right: 10px;
        opacity: 1;
      }

      .nav-links li {
        border-bottom: 1px solid var(--borde-navbar);
        text-align: right;
      }

      .nav-links li:last-child {
        border-bottom: none;
      }

      .nav-links li a {
        padding: 15px 20px;
        display: block;
      }
    }
  </style>
</head>
<body>

<nav class="navbar">
  <div class="navbar-container">
    <a href="/" class="logo">EncuestasCiudadanas</a>

    <input type="checkbox" id="menu-toggle" />
    <label for="menu-toggle" class="hamburger">
      <span></span>
      <span></span>
      <span></span>
    </label>

    <ul class="nav-links">
      <li><a href="/">Inicio</a></li>
      <% if (usuario && (usuario.rol==='administrador')) { %>
        <li><a href="/admin/usuarios">Usuarios</a></li>
      <% } %>
      <li><a href="/encuestas">Participar</a></li>
      <% if (usuario && usuario.rol==='entidad') { %>
        <li><a href="/encuestas/crear">Crear Encuestas</a></li>
      <% } %>
      <li>
        <% if (usuario) { %>
          <div class="dropdown">
            <button class="dropbtn">👤 <%= usuario.nombre %></button>
            <div class="dropdown-content">
              <a href="/perfil">Mi Perfil</a>
              <% if (usuario.rol==='entidad') { %>
                <a href="/encuestas/mis_encuestas">Mis Encuestas</a>
              <% } %>
              <a href="/logout">Cerrar sesión</a>
            </div>
          </div>
        <% } else { %>
          <a href="/login" class="btn">Login</a>
          <a href="/registro" class="btn">Registro</a>
        <% } %>
      </li>
    </ul>
  </div>
</nav>

<script>
  // Cierra el menú al hacer clic en cualquier enlace (solo móviles)
  document.querySelectorAll('.nav-links a').forEach(link => {
    link.addEventListener('click', () => {
      document.getElementById('menu-toggle').checked = false;
    });
  });
</script>

</body>
</html>
