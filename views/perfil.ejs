<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>perfil</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
  <%- include('partials/navbar') %>

<section class="max-w-3xl mx-auto mt-10 bg-white shadow-lg rounded-lg p-6">
  <h2 class="text-2xl font-semibold mb-4 border-b pb-2">👤 Información del Perfil</h2>

  <table class="w-full text-sm text-left border-separate space-y-2">
    <tbody class="divide-y">
      <tr>
        <th class="py-2 pr-4 text-gray-600 font-medium w-1/3">Nombre o Razón Social:</th>
        <td class="py-2 text-gray-800"><%= usuario.nombre %></td>
      </tr>
      <tr>
        <th class="py-2 pr-4 text-gray-600 font-medium">NIT:</th>
        <td class="py-2 text-gray-800"><%= usuario.identificacion %></td>
      </tr>

      <% if (usuario.rol === 'entidad') { %>
        <tr>
          <th class="py-2 pr-4 text-gray-600 font-medium">Tipo de Organización:</th>
          <td class="py-2 text-gray-800"><%= usuario.tipoOrganizacion %></td>
        </tr>
        <tr>
          <th class="py-2 pr-4 text-gray-600 font-medium">Dirección:</th>
          <td class="py-2 text-gray-800"><%= usuario.direccion %></td>
        </tr>
        <tr>
          <th class="py-2 pr-4 text-gray-600 font-medium">Teléfono:</th>
          <td class="py-2 text-gray-800"><%= usuario.telefono %></td>
        </tr>
      <% } else if (usuario.rol === 'usuario') { %>
        <tr>
          <th class="py-2 pr-4 text-gray-600 font-medium">Edad:</th>
          <td class="py-2 text-gray-800"><%= usuario.edad %></td>
        </tr>
        <tr>
          <th class="py-2 pr-4 text-gray-600 font-medium">Sexo:</th>
          <td class="py-2 text-gray-800"><%= usuario.sexo %></td>
        </tr>
        <tr>
          <th class="py-2 pr-4 text-gray-600 font-medium">Departamento:</th>
          <td class="py-2 text-gray-800"><%= usuario.departamento %></td>
        </tr>
        <tr>
          <th class="py-2 pr-4 text-gray-600 font-medium">Ocupación:</th>
          <td class="py-2 text-gray-800"><%= usuario.ocupacion %></td>
        </tr>
      <% } %>

      <tr>
        <th class="py-2 pr-4 text-gray-600 font-medium">Email:</th>
        <td class="py-2 text-gray-800"><%= usuario.email %></td>
      </tr>
    </tbody>
  </table>
</section>
</body>